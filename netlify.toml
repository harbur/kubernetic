[build]
  command = "yarn build"
  publish = "docs/.vuepress/dist"

# Keep internal links as VuePress writes them (/page.html), matching the
# canonical URLs. Netlify's pretty URLs rewrote them to /page.
[build.processing.html]
  pretty_urls = false
